{% extends 'base.html.twig' %}

{% block title %}Modifier la commande{% endblock %}

{% block body %}
<div class="max-w-3xl mx-auto p-6">

    <div class="bg-white shadow rounded-lg p-6">
        <h1 class="text-2xl font-bold mb-6">Modifier la commande</h1>

        {{ include('order/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('app_order_index') }}" class="mt-4 inline-block bg-gray-200 px-4 py-2 rounded hover:bg-gray-300">
            Retour à la liste
        </a>
    </div>

</div>
{% endblock %}
